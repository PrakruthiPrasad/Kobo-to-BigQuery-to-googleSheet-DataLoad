services:
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    command: >
      pytest tests/ -v
      --ignore=tests/test_fetcher.py
      --cov=shared
      --cov=services
      --cov-report=term-missing
      --cov-fail-under=50